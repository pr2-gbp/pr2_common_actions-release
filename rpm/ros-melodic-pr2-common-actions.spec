Name:           ros-melodic-pr2-common-actions
Version:        0.0.11
Release:        0%{?dist}
Summary:        ROS pr2_common_actions package

Group:          Development/Libraries
License:        BSD
URL:            http://wiki.ros.org/pr2_common_actions
Source0:        %{name}-%{version}.tar.gz

BuildArch:      noarch

Requires:       ros-melodic-joint-trajectory-action-tools
Requires:       ros-melodic-joint-trajectory-generator
Requires:       ros-melodic-pr2-arm-move-ik
Requires:       ros-melodic-pr2-common-action-msgs
Requires:       ros-melodic-pr2-tilt-laser-interface
Requires:       ros-melodic-pr2-tuck-arms-action
BuildRequires:  ros-melodic-catkin

%description
Various actions which help in moving the arms of the PR2 or getting data from
its tilting laser.

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/melodic/setup.sh" ]; then . "/opt/ros/melodic/setup.sh"; fi
mkdir -p obj-%{_target_platform} && cd obj-%{_target_platform}
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_LIBDIR="lib" \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/melodic" \
        -DCMAKE_PREFIX_PATH="/opt/ros/melodic" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/melodic/setup.sh" ]; then . "/opt/ros/melodic/setup.sh"; fi
cd obj-%{_target_platform}
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/melodic

%changelog
* Sat Sep 22 2018 ROS Orphaned Package Maintainers <ros-orphaned-packages@googlegroups.com> - 0.0.11-0
- Autogenerated by Bloom

