Name:           ros-hydro-joint-trajectory-generator
Version:        0.0.3
Release:        0%{?dist}
Summary:        ROS joint_trajectory_generator package

Group:          Development/Libraries
License:        BSD
URL:            http://ros.org/wiki/joint_trajectory_generator
Source0:        %{name}-%{version}.tar.gz

Requires:       ros-hydro-actionlib
Requires:       ros-hydro-angles
Requires:       ros-hydro-joint-trajectory-action
Requires:       ros-hydro-orocos-kdl
Requires:       ros-hydro-roscpp
Requires:       ros-hydro-urdf
BuildRequires:  ros-hydro-actionlib
BuildRequires:  ros-hydro-angles
BuildRequires:  ros-hydro-catkin
BuildRequires:  ros-hydro-joint-trajectory-action
BuildRequires:  ros-hydro-orocos-kdl
BuildRequires:  ros-hydro-roscpp
BuildRequires:  ros-hydro-urdf

%description
joint_trajectory_generator action takes in a trajectory specified by a number of
joint positions, and it generates a new smooth trajectory through these joint
positions.

%prep
%setup -q

%build
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/hydro/setup.sh" ]; then . "/opt/ros/hydro/setup.sh"; fi
mkdir -p build && cd build
%cmake .. \
        -UINCLUDE_INSTALL_DIR \
        -ULIB_INSTALL_DIR \
        -USYSCONF_INSTALL_DIR \
        -USHARE_INSTALL_PREFIX \
        -ULIB_SUFFIX \
        -DCMAKE_INSTALL_PREFIX="/opt/ros/hydro" \
        -DCMAKE_PREFIX_PATH="/opt/ros/hydro" \
        -DSETUPTOOLS_DEB_LAYOUT=OFF \
        -DCATKIN_BUILD_BINARY_PACKAGE="1" \

make %{?_smp_mflags}

%install
# In case we're installing to a non-standard location, look for a setup.sh
# in the install tree that was dropped by catkin, and source it.  It will
# set things like CMAKE_PREFIX_PATH, PKG_CONFIG_PATH, and PYTHONPATH.
if [ -f "/opt/ros/hydro/setup.sh" ]; then . "/opt/ros/hydro/setup.sh"; fi
cd build
make %{?_smp_mflags} install DESTDIR=%{buildroot}

%files
/opt/ros/hydro

%changelog
* Sat Sep 06 2014 Austin Hendrix <ahendrix@willowgarage.com> - 0.0.3-0
- Autogenerated by Bloom

